@using System.Collections
@using Kendo.Mvc.UI

@(Html.Kendo().DropDownListFor(m => m)
            .Name("MGRowid")
            .DataValueField("MGRowid")
            .DataTextField("MGName")
            .SelectedIndex(0)
            .DataSource(ds => ds
                .Read(read => read.Action("GetMGData", "UserData"))
            )
            .Filter("contains")
)